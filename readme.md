Django + DRF + Celery + Redis + PostgreSQL + Docker

Небольшой учебный сервис для асинхронной обработки задач.
REST API позволяет создавать задачи разных типов, а Celery-воркер обрабатывает их в фоне.

Проект полностью упакован в Docker и готов к развёртыванию.